---
schema_version: 1
id: OPP-20260828-d3lk
display_number: 1
context: saldo
verb: optimize
title: Ordenação determinística de locks select_for_update para prevenção de deadlock
target:
  files:
    - backend/apps/saldo/services.py
  symbol: SaldoService.transferir, SaldoService.migrar_saldo_contratos_vencidos, SaldoService.compensar_debito_contrato_anterior
smell: Aquisição de lock pessimista em ordem não determinística (origem -> destino) sob concorrência bidirecional pode gerar deadlock relacional em PostgreSQL.
roi:
  confidence: green
  impact: Hotpath contábil crítico. Evita travamento de conexões de banco e exceções 500 em transações simultâneas de transferências de saldo.
  cost: low
  est_return: Elimina risco de deadlock em transferências concorrentes mantendo a semântica atômica do saldo.
state: applied
traceability:
  soul: []
  specs:
    - _reversa_sdd/saldo/requirements.md#rf-sal-03
    - _reversa_sdd/domain.md#rn-04
---

### Problema Observado

Nos métodos `transferir`, `migrar_saldo_contratos_vencidos` e `compensar_debito_contrato_anterior` de `SaldoService`, o bloqueio de linha via `select_for_update()` é realizado na ordem em que os parâmetros são recebidos:

```python
c_origem = Contrato.objects.select_for_update().get(id=contrato_origem_id)
c_destino = Contrato.objects.select_for_update().get(id=contrato_destino_id)
```

Se uma transação A transferir horas do Contrato 1 para o Contrato 2, enquanto simultaneamente uma transação B transferir do Contrato 2 para o Contrato 1:
- A trava o Contrato 1 e aguarda o Contrato 2
- B trava o Contrato 2 e aguarda o Contrato 1
- Resultado: **Deadlock** no banco de dados.

### Transformação Proposta

Adquirir os locks dos contratos em uma única query com ordenação determinística pelo ID do contrato, ou ordenar as requisições de lock de forma previsível (ex: ordenando os UUIDs/IDs antes de travar as instâncias no banco).
